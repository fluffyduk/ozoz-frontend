services:
  frontend:
    build:
      context: .
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    environment:
      VITE_DOMAIN: ${VITE_DOMAIN:-http://localhost:5000}
      VITE_ORDERS_API_URL: ${VITE_ORDERS_API_URL:-http://localhost:5001}
